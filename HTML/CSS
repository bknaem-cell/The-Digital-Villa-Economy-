body {
    background: radial-gradient(circle at center, #001a2c 0%, #000000 100%);
    color: #ffffff;
    font-family: 'Arial', sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    margin: 0;
}

.hero-title {
    font-size: 3.5rem;
    font-weight: bold;
    color: #ffcc00;
    text-shadow: 0 0 10px #ffcc00, 0 0 30px #ffae00;
    margin: 10px 0;
}

.card {
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(15px);
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 40px;
    text-align: center;
    box-shadow: 0 10px 40px rgba(0,0,0,0.5);
}

.progress-container {
    width: 100%;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 50px;
    height: 10px;
    margin: 20px 0;
}

.progress-bar {
    width: 70%;
    height: 100%;
    background: linear-gradient(90deg, #00ff88, #00ffee);
    box-shadow: 0 0 15px #00ff88;
    border-radius: 50px;
}

.btn-enter {
    background: linear-gradient(45deg, #ffcc00, #ff9900);
    border: none;
    padding: 12px 25px;
    border-radius: 50px;
    color: #000;
    font-weight: bold;
    margin-top: 20px;
    box-shadow: 0 0 15px rgba(255, 204, 0, 0.5);
}
